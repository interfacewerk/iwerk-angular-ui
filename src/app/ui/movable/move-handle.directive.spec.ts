import { MoveHandleDirective } from './move-handle.directive';

describe('MoveHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveHandleDirective();
    expect(directive).toBeTruthy();
  });
});
