import { SelectLabelDirective } from './select-label.directive';

describe('SelectLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectLabelDirective();
    expect(directive).toBeTruthy();
  });
});
