import { CodeSamplePipe } from './code-sample.pipe';

describe('CodeSamplePipe', () => {
  it('create an instance', () => {
    const pipe = new CodeSamplePipe();
    expect(pipe).toBeTruthy();
  });
});
